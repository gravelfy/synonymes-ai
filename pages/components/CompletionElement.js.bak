import updateQuery from '../App.js';

export default function CompletionElement(props) {
  // constructor(props) {
  //   super(props);
  //   this.state = {
  //     elements: this.props.elements,
  //     result: this.props.result,
  //     query: this.props.query,
  //   };
  // }

  // render() {
  return (
    <>
      <div
        href="/"
        key={props.element}
        onClick={() => {
          updateQuery(props.element);
        }}
      >
        {' ===>  '}

        {props.element}
      </div>
    </>
  );
  // }
}
