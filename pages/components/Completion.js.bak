import CompletionElement from './CompletionElement';

export default function Completion(props) {
  //   constructor(props) {
  //     super(props);
  //     this.state = {
  //       elements: this.props.elements,
  //       result: this.props.result,
  //       query: this.props.query,
  //     };
  //   }

  //   render() {
  // console.log('--------************----------');
  // console.log(this.props.query);
  return (
    <>
      <div>{props.query}</div>
      <div>
        {props.elements?.map((item) => (
          <CompletionElement element={item}>{item}</CompletionElement>
        ))}
      </div>
    </>
  );
}
// }
